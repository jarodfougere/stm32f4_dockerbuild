#include <string.h>
#include <time.h> /* eventually we want to add timestamps to payloads */

#include "payloads.h"
#include "system_config.h"

/*
typedef struct
{
    int8_t  firmware_id[8];
    int8_t  hardware_id[8];
    int8_t  device_uid[13];
    uint8_t num_inputs;
    uint8_t num_relays;
    uint8_t num_batteries;
}   device_payload_t;

const device_payload_t device_payload = 
{
};

*/
